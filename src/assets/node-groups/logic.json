{
  "name": "逻辑",
  "color": "#E8B495",
  "nodes": [
    {
      "name": "与",
      "opcode": "and",
      "ports": {
        "in": ["in1", "in2"],
        "out": ["out1"]
      },
      "params": [
        {
          "label": "运算精度",
          "type": "string",
          "default": "bool"
        },
        {
          "label": "是否使用短路求值",
          "type": "boolean",
          "default": true
        }
      ]
    },
    {
      "name": "或",
      "opcode": "or",
      "ports": {
        "in": ["in1", "in2"],
        "out": ["out1"]
      },
      "params": [
        {
          "label": "运算精度",
          "type": "string",
          "default": "bool"
        },
        {
          "label": "是否使用短路求值",
          "type": "boolean",
          "default": true
        }
      ]
    }
  ]
}